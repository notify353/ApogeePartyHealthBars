# Distribution repository instructions

- Verify directory, branch, remote and dirty state before mutations. Preserve user changes, archives and backups. Use a short-lived codex branch, never a release tag.
- Read README.md, distribution/DUAL_WORKFLOW.md and RELEASING.md. This repository owns packaging/loading safety only. Never restore legacy APHB gameplay; independent child addons own gameplay.
- Before API-dependent work read docs/WOW_INTERFACE_EXPORT.md and run scripts/check-wow-api-export.ps1. Matching Forever16001 local export is authoritative. Stop when stale/missing; never substitute another client.
- One reviewed immutable source per child yields PROD/DEV. Update only reviewed pins/hashes; preserve audited lexical transforms. No blanket string substitutions. Gate every runtime chunk before side effects. Markers remain zero-runtime and zero-saved-data.
- Run pwsh ./scripts/test-local.ps1. Meaningful child gameplay checks must use generated outputs through owning tasks when transforms change. Mocks do not establish native timing, taint, secure UI or live acceptance.
- Routine local installs target DEV only through scripts/install_dual_distribution.py. Child tasks never overwrite canonical PROD. Do not open/import/copy private saved data. Preserve documentation, unknown files and enable preferences; refuse unexpected runtime edits, verify backups and installed bytes.
- Initial PROD safety retrofit is a one-time owner-authorized exception, not permission for routine PROD overwrites. Canonical guidance routes here until integration. Do not operate/restart WoW or change enabled states; user switches groups and reloads.
- Preserve CurseForge1608100/GitHub identity. No push, main integration, tag, release, upload, registration, account/security change or external message without explicit authorization. Use GitHub CLI for GitHub operations.
- Offline candidate locks never authorize publication. The reviewed Actions publisher requires accepted runtime bytes, hosted source access and exact Forever version checks. Production tags require explicit approval under the owner's current instructions. Actions must publish identical verified PROD bytes to both services; never include DEV roots.
- Add changes to CHANGELOG.md Unreleased. Historical entries describe archived versions. No destructive repository/worktree/backup cleanup.
- Stable central workflow: C:/Dev/WoW/ApogeePartyHealthBars/distribution/DUAL_WORKFLOW.md. Child tasks request reviewed pin updates and DEV installs here; retained prototype worktrees are not the active authority.
- Owner policy: canonical PROD folders are installed only by the CurseForge app. The staged candidate was moved to a verified handover backup; never locally reinstall PROD ZIPs, repeat the initial retrofit, or restore that backup without new explicit authorization. Normal local work installs DEV only.
