name: Production release gate
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - name: Publication disabled pending distribution acceptance
        run: |
          echo "No publishing is enabled. Complete RELEASING.md gates before implementing exact-byte Actions publication."
          exit 1
